import React from "react";
import { TrackInfo } from "./utilities/types";

type TrackGridProps = {
	tracks: Array<TrackInfo>;
};

function TrackGrid({ tracks }: TrackGridProps) {
	return <div></div>;
}

export default TrackGrid;
